# 快速参考 - npm 脚本命令

## 🚀 快速开始

### 第一次运行
```bash
npm install           # 安装依赖
npm run preprocess    # 预处理所有笔记
npm run validate      # 生成验证报告
npm run build         # 构建网站
```

### 日常开发
```bash
npm run dev      # 启动开发服务器（自动监听变化）
```

---

## 📋 所有可用命令

| 命令 | 功能 | 输出 |
|------|------|------|
| `npm run preprocess:frontmatter` | 统一front matter | 修复/补充元数据 |
| `npm run preprocess:links` | 转换Obsidian链接 | [[]] → [] 格式 |
| `npm run preprocess:math` | 验证和修复公式 | 修复空格/括号问题 |
| `npm run preprocess` | 运行全部预处理 | 完成上述3个步骤 |
| `npm run validate` | 生成验证报告 | validation-report.json |
| `npm run build` | 生成HTML网站 | public/ 目录 |
| `npm run dev` | 开发服务器 | 自动重载 |
| `npm run serve` | 生产服务器 | 无自动重载 |

---

## 📊 验证报告指标

运行 `npm run validate` 查看：

```
📊 总统计
  总文件数: 18
  完整front matter: 18
  缺失字段: 0
  断链数: 4
  公式问题: 4
```

---

## 🔧 常见任务

### 添加新笔记
```bash
# 1. 在 src/notes/ 创建 .md 文件
# 2. 运行预处理：
npm run preprocess

# 3. 查看报告：
npm run validate
```

### 修复所有问题
```bash
npm run preprocess    # 自动修复大多数问题
npm run validate      # 检查剩余问题
```

### 检查特定问题
```bash
npm run validate      # 生成详细报告
cat validation-report.json  # 查看完整细节
```

### 构建和部署
```bash
npm run build         # 生成 public/ 目录
# 将 public/ 目录内容部署到web服务器
```

---

## ✅ Front Matter 模板

```yaml
---
title: "笔记标题"
layout: "note.njk"
date: "YYYY-MM-DD"
permalink: "/notes/笔记-标题/index.html"
tags: []
---
```

---

## 🔗 链接格式

| 格式 | 用途 | 例子 |
|------|------|------|
| `[[文件名]]` | 基本链接 | `[[项目计划]]` |
| `[[文件名\|显示文本]]` | 自定义显示 | `[[项目计划\|点击查看计划]]` |
| `[[文件名#标题]]` | 链接到标题 | `[[笔记#第二章]]` |

自动转换为：
```markdown
[文件名](/notes/文件-名/)
[显示文本](/notes/文件-名/)
[文件名 (第二章)](/notes/文件-名/)
```

---

## 📝 公式格式

| 类型 | 格式 | 例子 |
|------|------|------|
| 内联公式 | `$...$` | `$E=mc^2$` |
| 显示公式 | `$$...$$` (单独行) | 见下面 |

**显示公式示例:**
```markdown
$$
E=mc^2
$$
```

**❌ 不推荐:**
```markdown
这是公式 $$ E=mc^2 $$
使用 \( E=mc^2 \)
使用 \[ E=mc^2 \]
```

---

## ⚠️ 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 断链警告 | 链接指向不存在的文件 | 创建缺失的笔记或修改链接 |
| 公式显示错误 | 空格或符号不匹配 | 运行 `npm run preprocess:math` |
| Front matter 不完整 | 缺少必要字段 | 运行 `npm run preprocess:frontmatter` |
| 中文标题乱码 | 终端编码问题 | 使用Node.js读取验证实际内容 |

---

## 📁 项目文件

```
scripts/
├── add-frontmatter.js         # Front Matter统一
├── convert-links.js           # 链接转换
├── validate-mathjax.js        # 公式验证
└── validate-and-report.js     # 报告生成

validation-report.json          # 最后一次验证报告
SCRIPTS_GUIDE.md                # 详细文档（本文件）
```

---

## 📚 更多信息

详见 `SCRIPTS_GUIDE.md`